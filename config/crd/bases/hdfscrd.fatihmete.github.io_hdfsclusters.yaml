---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: hdfsclusters.hdfscrd.fatihmete.github.io
spec:
  group: hdfscrd.fatihmete.github.io
  names:
    kind: HDFSCluster
    listKind: HDFSClusterList
    plural: hdfsclusters
    singular: hdfscluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: HDFSCluster is the Schema for the hdfsclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HDFSCluster
            properties:
              name:
                description: Unique name of the HDFS cluster
                type: string
              nodes:
                description: |-
                  Nodes is a list of HDFS nodes in the cluster
                  Each node should have a unique name and a valid hostname
                items:
                  properties:
                    disks:
                      description: |-
                        Disks is a list of disk paths used by the HDFS node
                        Each disk path should be a valid filesystem path where HDFS can store data
                      items:
                        type: string
                      minItems: 1
                      type: array
                    hostname:
                      description: Hostname of the HDFS node
                      type: string
                    name:
                      description: Name of the HDFS node
                      type: string
                    role:
                      description: Role of the HDFS node (e.g., NameNode, DataNode)
                      type: string
                  required:
                  - disks
                  - hostname
                  - name
                  - role
                  type: object
                minItems: 1
                type: array
            required:
            - name
            - nodes
            type: object
          status:
            description: status defines the observed state of HDFSCluster
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
